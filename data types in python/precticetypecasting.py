#Take two inputs from the user (they come as strings by default), convert them to floats, and print their sum, difference, and product.
# Given nums = ["1", "2", "3", "4", "5"], use a loop or list comprehension to convert all elements to integers and find their sum.
#Given data = [1, 2.5, True, "10"], convert every element to a float and print the results.
# Accept temperature as a string (e.g., "98.6"), convert to float, then compute Celsius using (F - 32) * 5/9.
#Write a program that tries to convert a user-inputted string to an integer. If it fails, print "Invalid input!" using a try/except block.